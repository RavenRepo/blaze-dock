# BlazeDock Configuration
# 
# This is the default configuration file for BlazeDock.
# Copy this to ~/.config/blazedock/blazedock.toml to customize.

# Dock position: "left", "right", "top", or "bottom"
position = "bottom"

# Icon size in pixels
icon_size = 48

# Dock width/height (depending on orientation)
dock_size = 72

# Margin from screen edge in pixels
margin = 8

# Spacing between icons in pixels
spacing = 8

# Enable auto-hide behavior
auto_hide = false

# Auto-hide delay in milliseconds
auto_hide_delay = 500

# Background opacity (0.0 - 1.0)
opacity = 0.85

# Border radius for rounded corners
border_radius = 16

# Enable exclusive zone (push windows aside)
exclusive_zone = false

# Enable hover zoom effect
hover_zoom = true

# Hover zoom scale factor
hover_zoom_scale = 1.15

# =============================================================================
# Pinned Applications
# 
# Customize these based on your desktop environment:
# - KDE: dolphin, konsole, systemsettings
# - GNOME: nautilus, gnome-terminal, gnome-control-center
# - Generic: xdg-open, x-terminal-emulator
# =============================================================================

[[pinned_apps]]
name = "Firefox"
icon = "firefox"
command = "firefox"
desktop_file = "/usr/share/applications/firefox.desktop"

# --- KDE Desktop (uncomment for KDE/Plasma) ---
[[pinned_apps]]
name = "Files"
icon = "org.kde.dolphin"
command = "dolphin"
desktop_file = "/usr/share/applications/org.kde.dolphin.desktop"

[[pinned_apps]]
name = "Terminal"
icon = "org.kde.konsole"
command = "konsole"
desktop_file = "/usr/share/applications/org.kde.konsole.desktop"

[[pinned_apps]]
name = "Settings"
icon = "systemsettings"
command = "systemsettings"
desktop_file = "/usr/share/applications/systemsettings.desktop"

# --- GNOME Desktop (uncomment for GNOME) ---
# [[pinned_apps]]
# name = "Files"
# icon = "org.gnome.Nautilus"
# command = "nautilus"
# desktop_file = "/usr/share/applications/org.gnome.Nautilus.desktop"

# [[pinned_apps]]
# name = "Terminal"
# icon = "org.gnome.Terminal"
# command = "gnome-terminal"
# desktop_file = "/usr/share/applications/org.gnome.Terminal.desktop"

# [[pinned_apps]]
# name = "Settings"
# icon = "org.gnome.Settings"
# command = "gnome-control-center"
# desktop_file = "/usr/share/applications/org.gnome.Settings.desktop"

# --- Common Apps ---
[[pinned_apps]]
name = "Code"
icon = "visual-studio-code"
command = "code"
